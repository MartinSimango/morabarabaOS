#include "util.h"

// static inline void outb(uint16 port, uint8 value) {
//     asm ("outb %0, %1" : : "r" (value), "dN" (port));
// }

// static inline uint8 inb(uint16 port) {
//     uint8 value;
//     asm ("inb %1, %0" : "=r" (value) : "dN" (port));
//     return value;
// }
